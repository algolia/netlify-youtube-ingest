[template.environment]
  YOUTUBE_KEY = "Your YouTube API Developer key"
  VITE_ALGOLIA_APP_ID = "The App in Algolia where your index is"
  ALGOLIA_API_KEY = "The Algolia admin API Key"
  VITE_VIDEO_INDEX = "The Index name (i.e. video_index)"
  VITE_ALGOLIA_SEARCH_KEY="Search-only key. Optional. For the included small frontend"

[build]
  command = "npm run build"
  publish = "dist"